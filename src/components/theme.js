// Crear el tema personalizado para el componente de filtros y el boton de buscar.
// Requiero que se vean exactamente como en el figma, tanto los botones como los select
// Por lo tanto necesito que crees todo desde el theme, con los colores y las variantes

import { createTheme } from "@mui/material";


export const theme = createTheme({
    palette: {
        primary: {
            main: '#FDD000'
        },
        secondary: {
            main: '#FDD000'
        }
    }
})